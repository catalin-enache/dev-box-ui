{
  "name": "dev-box-ui",
  "version": "0.0.11",
  "private": true,
  "author": "catalinenache78@gmail.com",
  "license": "MIT",
  "main": "./dist/src/lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/catalin-enache/dev-box-ui.git"
  },

  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "cross-env": "^5.0.1",
    "loose-envify": "^1.3.1",
    "uglify-es": "^3.0.25"
  },
  "dependencies": {

  },
  "peerDependencies": {
    "prop-types": "^15.5.10",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "scripts": {
    "build:vendors:dev":  "                               browserify -r react -r react-dom -r prop-types                        -o demo/js/vendors.js",
    "build:vendors:prod": "cross-env NODE_ENV=production  browserify -r react -r react-dom -r prop-types                        -t [ loose-envify purge --NODE_ENV production ] | uglifyjs --compress=unused,dead_code -o demo/js/vendors.min.js",

    "build:lib:dev":   "                                  browserify -r ./src/lib/:dev-box-ui -x react -x react-dom -x prop-types          --debug -o demo/js/lib.js",
    "build:lib:prod":  "cross-env NODE_ENV=production     browserify -r ./src/lib/:dev-box-ui -x react -x react-dom -x prop-types          -t [ loose-envify purge --NODE_ENV production ] | uglifyjs --compress=unused,dead_code -o demo/js/lib.min.js",

    "build:demo": "                                       browserify -x react -x react-dom -x prop-types -x dev-box-ui         --debug -o demo/js/demo.js demo.js",


    "build:babel:dist": "babel ./src --out-dir ./dist/src --ignore *.spec.js"




  }
}
