{
  "name": "dev-box-ui",
  "version": "0.0.2",
  "private": true,
  "author": "catalinenache78@gmail.com",
  "license": "MIT",
  "main": "./dist/js/lib.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/catalin-enache/dev-box-ui.git"
  },
  "files": [
    "dist/",
    "src/"
  ],
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "cross-env": "^5.0.1",
    "loose-envify": "^1.3.1",
    "uglify-es": "^3.0.25"
  },
  "dependencies": {
    "prop-types": "^15.5.10",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "scripts": {
    "build:vendors:dev":  "                              browserify -r react -r react-dom -r prop-types -o vendors/vendors.js",
    "build:vendors:prod": "cross-env NODE_ENV=production browserify -r react -r react-dom -r prop-types -t [ loose-envify purge --NODE_ENV production ] | uglifyjs --compress=unused,dead_code -o vendors/vendors.min.js",

    "build:lib:dev":   "cross-env                     NODE_PATH=./src browserify -r lib -x react -x react-dom -x prop-types --debug -o dist/js/lib.js",
    "build:lib:prod":  "cross-env NODE_ENV=production NODE_PATH=./src browserify -r lib -x react -x react-dom -x prop-types -t [ loose-envify purge --NODE_ENV production ] | uglifyjs --compress=unused,dead_code -o dist/js/lib.min.js",

    "build:demo": "cross-env NODE_PATH=./src browserify -x react -x react-dom -x prop-types -x lib --debug -o demo/js/demo.js demo.js"



  }
}
